
credentials:
  - id: junk_shop_db
    type: name_and_password
  - id: service_email
    type: password

junk_shop:
  db_host: megatron

platforms:
  linux-x64:
    build_node: linux
  mac:
    build_node: mac
  win-x64:
    build_node: windows
