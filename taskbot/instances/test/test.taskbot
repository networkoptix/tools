# -*- sh -*-

set -o errexit +o nounset -o noclobber

ARGS="$TASKBOT_OPTIONS $TASKBOT_CONFIG"

echo $1
echo $2

$TASKBOT_COMMONS/core/file.py \
     $TASKBOT_CONFIG \
    --path $TASKBOT_COMMONS \
    --mask '*.taskbot' \
    --mask '*.py' \
    --exclude 'test*'

# + Test command
echo $TASKBOT
echo $TASKBOT_OPTIONS
echo $TASKBOT_CONFIG
echo $TASKBOT_PARENT_PID
echo $TASKBOT_REPO
echo $TASKBOT_PARENT_PID

for i in `seq 1 10`;
do
   echo $i
done

# + Sleep
sleep 100

#echo $ABRACADABRA
/such/no

echo Test
