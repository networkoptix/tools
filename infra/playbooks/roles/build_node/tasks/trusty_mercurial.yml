- name: Install Mercurial repository
  apt_repository:
    repo: ppa:mercurial-ppa/releases
    state: present
  register: add_mercurial_repo

- name: Install Mercurial
  apt:
    name: "{{ item }}"
    update_cache: "{{ add_mercurial_repo.changed }}"
    state: present
  with_items:
  - mercurial
