# List of allowed ip ranges for iptables settings
allowed_ips:
  # internal CI network (jenkins, prometheus, etc)
  - "192.168.13.0/24"
  # internal scalability network (mediaservers, scalability test environment)
  - "192.168.20.0/24"
  # Seva's host (debug purpose)
  - "10.0.3.91"
allowed_tcp_ports:
  - 22 # SSH
  - 10050 # Zabbix
allow_icmp: True