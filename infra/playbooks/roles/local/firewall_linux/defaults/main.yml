# List of allowed ip ranges for iptables settings
allowed_ips:
  - "192.168.20.0/24"  # internal scalability network (mediaservers, scalability test environment)
  - "192.168.13.7"  # Jenkins slave on delta
  - "192.168.13.119"  # Prometheus
  - "10.0.3.91"  # Seva's host (debug purpose)
  - "192.168.13.13"  # Seva's host (debug purpose)
allowed_tcp_ports:
  - 22 # SSH
  - 10050 # Zabbix
allow_icmp: True
