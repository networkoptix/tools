# it's just a helper to setup only node exporter to destination hosts
- name: Setup monitoring node
  hosts: all
  become: true
  become_method: sudo

  roles:
  - role: cloudalchemy.node-exporter
  - role: libvirt_exporter
    when:  groups['libvirt_host']