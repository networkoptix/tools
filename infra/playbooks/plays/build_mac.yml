- name: Setup macOS build machine
  hosts: build_mac
  gather_facts: yes

  vars_files:
  - ../secrets/infra_storage.yml

  roles:
  # FIXME: we need these shares attached but we can't create users on mac using
  # current playbooks
  # - role: samba_share
  #   become: true
  #   become_method: sudo
  #   when:
  #   - attach_infra_share | default(True) == True
  #   vars:
  #     samba_mount_pass: "{{ cinas_infra_password }}"
  # - role: beta_builds_share
  #   become: true
  #   become_method: sudo
  - osx_build_node
