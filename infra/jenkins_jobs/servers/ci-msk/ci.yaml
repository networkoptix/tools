- project:
    name: ci
    project-name: ci
    branch:
      - vms
      - vms_3.2_dev
    platform:
      - linux-x64
      - bpi
    jobs:
      - '{project-name}-{branch}'
      - '{project-name}-{branch}-webadmin'
      - '{project-name}-{branch}-{platform}'
