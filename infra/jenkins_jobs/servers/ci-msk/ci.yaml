- project:
    name: ci
    project-name: test
    branch:
      - vms
      - vms_3.2_dev
    platform:
      - linux-x64
    jobs:
      - '{project-name}-{branch}'
      - '{project-name}-{branch}-init-build'
      - '{project-name}-{branch}-webadmin'
      - '{project-name}-{branch}-{platform}'
