- project:
    name: scalability_test

    platform: linux-x64
    upstream-jenkins: http://jenkins.enk.me
    upstream-project: ci
    customization: hanwha

    # Scalability test is run on remote (burbank) host.
    # Junk-shop db host is mapped using 'ssh -R 5432:megatron:5432' by agent launch script.
    junk-shop-host: localhost

    jobs:
    - '{branch}-scalability':
        branch: vms_3.2_dev
    - '{branch}-scalability':
        branch: vms
