- project:
    name: functional_tests

    project-name: ci

    platform:
      - linux-x64
    branch:
      - vms_3.2
      - vms

    upstream-project: ci
    customization: default
    cloud-group: test
    binaries_url: 'rsync://noptix.enk.me/buildenv/test/'
    test-timeout-hours: 6
    job-timeout-minutes: 420  # 7 hours
    junk-shop-host: '10.0.0.113'

    # See extra/burbank-node-readme.txt for explanation
    # junk-shop-host: localhost

    jobs:
      - '{project-name}-{branch}-{platform}-funtest'
      - '{project-name}-{branch}-{platform}-funtest-debug':
          branch:
            - vms
