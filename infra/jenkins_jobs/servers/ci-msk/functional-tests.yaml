- project:
    name: functional_tests

    project-name: ci

    platform:
      - linux-x64
    branch:
      - vms_3.2
      - vms

    upstream-project: ci
    junk-shop-host: '10.0.0.113'
    customization: default
    cloud-group: test
    binaries_url: 'rsync://noptix.enk.me/buildenv/test/'
    test-timeout-hours: 3
    job-timeout-minutes: 300

    jobs:
      - '{project-name}-{branch}-{platform}-funtest'
