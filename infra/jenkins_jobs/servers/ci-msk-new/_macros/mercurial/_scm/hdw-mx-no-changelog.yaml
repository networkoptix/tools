

- scm:
    name: mercurial/hdw-mx-no-changelog
    scm:
    - hg:
        revision: '{branch-name}'
        url: 'ssh://hg@hdw.mx/{project-name}'
        credentials-id: nx-repository-readonly
        clean: true
        subdir: '{project-name}'
        disable-changelog: true
