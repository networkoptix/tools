# This is a special job-group for adding *.all wrapper for build and
# also unittest job inside this wrapper.
# It must be used for only those projects and configurations which are supported in runner.
- job-group:
    name: installer-build-with-unittests
    jobs:
    - installer-all
    - installer-unittests
