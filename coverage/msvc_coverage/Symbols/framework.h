#pragma once

#define WIN32_LEAN_AND_MEAN //< Exclude rarely-used stuff from Windows headers.
#define NOMINMAX //< Avoid errors with things like std::numeric_limits<>::max().

// Windows Header Files
#include <windows.h>
